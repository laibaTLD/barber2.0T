/** Next/Image default quality is 75 — too soft for large hero/gallery photos on retina displays. */
export const CMS_IMAGE_QUALITY = 95;

/** Gallery carousel — request enough pixels for ~1280px layout at 2× DPR. */
export const GALLERY_IMAGE_SIZES = '(min-width: 1280px) 1400px, 100vw';
